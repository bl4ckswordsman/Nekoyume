package com.bl4ckswordsman.nekoyume.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 Typography using native defaults Only customize when app-specific typography is needed
 * This follows Material 3's built-in type scale for optimal readability
 */
val Typography = Typography()

// If app-specific typography is needed later, extend like this:
// val Typography = Typography().copy(
//     headlineMedium = Typography().headlineMedium.copy(
//         fontFamily = YourCustomFontFamily
//     )
// )
